# GGEAttackBot

React Native Android test-overlay project with a GitHub Pages download site.

## Development

```powershell
npm install
npm run android
```

The static download site is in `site/` and is deployed by GitHub Pages through `.github/workflows/pages.yml`.

Place a built APK at `site/releases/GGEAttackBot.apk` before publishing a downloadable release.